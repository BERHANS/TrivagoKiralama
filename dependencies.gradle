ext {//bu yml dosyasını açmamıızn sebebi bu proje export edeceği için gerekli
    // Burada kullanmak istediğimiz bağımlılıkların versiyonlarını ekliyor olacağız
    // Eğer kullandığımız bağımlılıklarda yeni bir güncelleme geldiğinde sadece burdan değiştirerek kullanırız.
    //kullandığımız "versions" tamamen opsiyonel bir isimlendirmedir
    versions = [
        springBoot          : "3.1.5",
        springCloud         : "4.0.4",
        lombok              : "1.18.30",
        mapsturct           : "1.5.5.Final",
        swaggerui           : "2.1.0",
        postgresql          : "42.6.0",
        jwt                 : "4.4.0",
        CircuitBreaker      : "3.0.3"

    ]
    // Burada ise eklemek istediğimiz bağımlılıklar için bir liste oluşturuyoruz.
    libs =  [
            springBootStarterWeb        : "org.springframework.boot:spring-boot-starter-web:$versions.springBoot",
            //versions dan gelen ad yukarıda belirlediğimiz ad ile aynı olmalıdır.
            springBootDataJpa           : "org.springframework.boot:spring-boot-starter-data-jpa:$versions.springBoot",
            springBootDataMongoDB       : "org.springframework.boot:spring-boot-starter-data-mongodb:$versions.springBoot",
            springBootTest              : "org.springframework.boot:spring-boot-starter-test:$versions.springBoot",
            springCloudOpenFeign        : "org.springframework.cloud:spring-cloud-starter-openfeign:$versions.springCloud",
            swaggerui                   : "org.springdoc:springdoc-openapi-starter-webmvc-ui:$versions.swaggerui",
            lombok                      : "org.projectlombok:lombok:$versions.lombok",
            mapsturct                   : "org.mapstruct:mapstruct:$versions.mapsturct",
            mapsturctProcessor          : "org.mapstruct:mapstruct-processor:$versions.mapsturct",
            postgresql                  : "org.postgresql:postgresql:$versions.postgresql",
            jwt                         : "com.auth0:java-jwt:$versions.jwt",
            springCloudConfig           : "org.springframework.cloud:spring-cloud-starter-config:$versions.springCloud",
            springCloudConfigServer     : "org.springframework.cloud:spring-cloud-config-server:$versions.springCloud",
            springCloudConfigClient     : "org.springframework.cloud:spring-cloud-config-client:$versions.springCloud",
            springCloudGateway          : "org.springframework.cloud:spring-cloud-starter-gateway:$versions.springCloud",
            springCloudCircuitBreaker   :"org.springframework.cloud:spring-cloud-starter-circuitbreaker-resilience4j:$versions.CircuitBreaker",
            springCloudCircuitBreakerReactive  :"org.springframework.cloud:spring-cloud-starter-circuitbreaker-reactor-resilience4j:$versions.CircuitBreaker",

    ]
}
//burası kütüphanedir buraya eklenen bağımlılıklar tüm projeye yansımaz build.gradle kısmına eklemeliyiz
//fakat kullandığımız yapıda veritabanları farklı olacağı için kullanmak istediğimiz micro servisin build gradleına eklememiz gerekir